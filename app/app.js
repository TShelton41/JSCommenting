/**
 * This function will get the exact data you need from firebase.
 * @author Todd Shelton
 * @param {string} id - You need to pass over the exact comments id to get the right object
 * @param {object} comment - this is the entire comment for the post
 * @return {void}
 */

function getExactData(id, comment) {}
